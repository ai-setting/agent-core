{
  "name": "@agent-core/server",
  "version": "0.1.0",
  "description": "HTTP Server for agent-core with SSE support",
  "type": "module",
  "scripts": {
    "start": "bun run start.ts",
    "start:dev": "bun run start.ts --dev",
    "dev": "bun run src/index.ts",
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.6.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "bun-types": "latest",
    "typescript": "^5.3.0"
  }
}
